@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Помилка.</h1>
<h2 class="text-danger">Помилка під час обробки вашого запиту.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>ID Запиту:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Режим Розробки</h3>
<p>
    Перехід на Середовище <strong>Розробки</strong> забезпечить більш деталізовну інформацію про помилку що сталася.
</p>
<p>
    <strong>Середовище розробки не має бути ввімкнено для розгорнутих програм.</strong>
    Це може призвести до відображення конфіденційної інформації з винятків кінцевим користувачам.
    Для локального налагодження увімкніть середовище <strong>Розробки</strong>, встановивши для змінної середовища <strong>ASPNETCORE_ENVIRONMENT</strong> значення <strong>Development</strong>
    і перезапустіть програму.
</p>
